/**
 * 03)​ Crie uma função que recebe dois parâmetros,
 * base e expoente, e retorne a base elevada ao expoente.
 */

 function exp(base, expoente) {
     return Math.pow(base, expoente)
 }

 const base = 2
 const expoente = 8
 console.log(exp(base, expoente)) 